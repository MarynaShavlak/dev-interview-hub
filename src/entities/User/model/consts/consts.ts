/**
 * Enum representing user roles.
 *
 * @enum {string}
 * @property {string} ADMIN - Role for administrators.
 * @property {string} USER - Role for regular users.
 * @property {string} MANAGER - Role for managers.
 */

export enum UserRole {
    ADMIN = 'ADMIN',
    USER = 'USER',
    MANAGER = 'MANAGER',
}
